let arr:number[] = new Array(6,4,8,3,0,2,7);
/*function insertionSort(a:number[]){

    for (let i:number =1 ; i<a.length ; i++ )
    {
        let temp = a[i];

        for(let j:number =i ;j>0 && temp<a[j-1];j--){
            a[j]= a[j-1];
            a[j-1]= temp;

        }
    }
    console.log(arr);  //-1

} 

console.log(insertionSort(arr));  //
*/
/*
 function SelectionSorting(a:number[]){
     for (let i : number = 0 ; i <a.length ; i++){
         let  smallest: number =  i;
          for (let j:number = i+1; j<a.length ; j++)
          {4
               if(a[j]<a[smallest])
               smallest = j
          }
          let temp:number;
          temp = a[smallest];
          a[smallest]= a[i];
          a[i]= temp;
     }
     console.log(arr);  //-1
 } 
// let arr:number[] = new Array(6,4,8,3,0,2,7);
 console.log(SelectionSorting(arr));  // */

 